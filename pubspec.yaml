name: esc_pos_bluetooth
description: The library allows to print receipts using an ESC/POS thermal Bluetooth printer.
publish_to: none
version: 0.2.7
homepage: https://github.com/andrey-ushakov/esc_pos_bluetooth

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  rxdart: ^0.26.0

  esc_pos_utils:
    git:
      url: https://github.com/RangerOfGondor/esc_pos_utils.git
      
  flutter_bluetooth_basic: 
    # path: ../flutter_bluetooth_basic
    git:
      url: https://github.com/RangerOfGondor/flutter_bluetooth_basic.git

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
